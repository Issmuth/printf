#include "main.h"
/**
 * _strlen - returns the lenght of a string
 * @str: array of chars
 * Return: length of the array of chars
 */
int _strlen(char *str)
{
	unsigned int i = 0;

	while (*s++)
	{
		i++;
	}
	return (i);
}
